interface:
  display_name: "Frontend Responsive Layout"
  short_description: "Fix breakpoints, containers, overflow, media, and touch"
  default_prompt: "Use $frontend-responsive-layout to inspect the current page or component, then fix viewport and container behavior, sizing constraints, overflow, responsive media, safe areas, and touch layout."
