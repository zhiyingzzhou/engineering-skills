interface:
  display_name: "Frontend Accessibility"
  short_description: "Fix semantics, keyboard, focus, ARIA, and AT gaps"
  default_prompt: "Use $frontend-accessibility to inspect the current frontend UI, then improve semantic HTML, keyboard operation, focus management, ARIA state, and assistive-technology behavior."
