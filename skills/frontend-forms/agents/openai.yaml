interface:
  display_name: "Frontend Forms"
  short_description: "Improve labels, validation, errors, autofill, and privacy"
  default_prompt: "Use $frontend-forms to inspect the current form flow, then improve native controls, labels, validation timing, error handling, autofill, password-manager behavior, and privacy-sensitive input."
